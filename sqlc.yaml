version: "2"
sql:
  - engine: "postgresql"
    schema: "internal/db/schema"
    queries: "internal/db/queries/"
    codegen:
      codegen:
      out: internal/db/store
      package: store
      emit_interface: true
      emit_json_tags: true
